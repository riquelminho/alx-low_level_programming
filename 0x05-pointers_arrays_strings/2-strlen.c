#include "main.h"

/**
 * _strlen - check the code
 * @s: string to evaluate
 *
 * Return: the length of the string
 */
int _strlen(char *s)
{
 int i;
    for(i = 0; str[i] != '\0'; i++);
    return i;
}
